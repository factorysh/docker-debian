---

command:
  "cat /etc/debian_version":
    exit-status: 0
    stdout:
      - bullseye

  "cat /etc/timezone":
    exit-status: 0
    stdout:
      - Europe/Paris

  "which vi":
    exit-status: 0
    stdout:
      - /usr/bin/vi
